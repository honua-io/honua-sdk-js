{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://honua.io/schemas/competitor-evidence.v1.json",
  "title": "Honua competitor evidence record set (v1)",
  "description": "Structured, immutable-per-observation evidence backing every external claim projected into docs/comparison.md (honua-io/honua-sdk-js#499, REQ-005/REQ-006, NFR-002). Refreshing a claim adds a new observation; it never rewrites an existing record's provenance.",
  "type": "object",
  "additionalProperties": false,
  "required": ["format", "categories", "records"],
  "properties": {
    "$schema": { "type": "string" },
    "format": { "const": "honua.sdk.competitor-evidence.v1" },
    "note": { "type": "string" },
    "categories": {
      "description": "Category boundaries. A comparison may only place products in the same table when they share a category, or when the rendered page names the boundary explicitly (REQ-002).",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": ["label", "definition"],
        "properties": {
          "label": { "type": "string", "minLength": 1 },
          "definition": { "type": "string", "minLength": 1 }
        }
      }
    },
    "records": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/record" }
    }
  },
  "$defs": {
    "date": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    },
    "operationCell": {
      "type": "object",
      "additionalProperties": false,
      "required": ["support"],
      "properties": {
        "support": {
          "description": "✓ first-party support, ◐ partial or caller-assembled, — not provided.",
          "enum": ["✓", "◐", "—"]
        },
        "note": {
          "description": "Short qualifier rendered in the cell, e.g. what the caller still has to assemble.",
          "type": "string",
          "minLength": 1
        }
      }
    },
    "metric": {
      "type": "object",
      "additionalProperties": false,
      "required": ["key", "label", "value", "unit", "compression"],
      "properties": {
        "key": { "type": "string", "minLength": 1 },
        "label": { "type": "string", "minLength": 1 },
        "value": { "type": "string", "minLength": 1 },
        "unit": {
          "description": "The metric unit. Two metrics may only be combined or ranked against each other when unit AND compression match (NFR-002).",
          "enum": ["MB", "KiB", "bytes", "files", "count", "seconds", "release-line"]
        },
        "compression": {
          "enum": ["none", "gzip", "brotli", "not-applicable"]
        }
      }
    },
    "supportingSource": {
      "type": "object",
      "additionalProperties": false,
      "required": ["url", "sourceType", "supports"],
      "properties": {
        "url": { "type": "string", "format": "uri" },
        "sourceType": { "const": "primary" },
        "supports": {
          "description": "The exact part of the record this additional primary source supports.",
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "enum": ["claim", "operations", "comparability", "versionLine", "metric:latestVersion"]
          }
        }
      }
    },
    "record": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "product",
        "package",
        "category",
        "versionLine",
        "claim",
        "sourceUrl",
        "sourceType",
        "observedAt",
        "retrievedAt",
        "expiresAt",
        "methodology",
        "metrics",
        "comparability"
      ],
      "properties": {
        "id": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$" },
        "product": { "type": "string", "minLength": 1 },
        "package": {
          "description": "The exact package the record represents. A comparison must never substitute a narrower proxy package for one competitor only (REQ-002).",
          "type": "string",
          "minLength": 1
        },
        "category": { "type": "string", "minLength": 1 },
        "versionLine": {
          "description": "The product version or release line the observation applies to. Required: an undated, unversioned external claim is not admissible evidence (REQ-005).",
          "type": "string",
          "minLength": 1
        },
        "claim": { "type": "string", "minLength": 1 },
        "sourceUrl": { "type": "string", "format": "uri" },
        "sourceType": {
          "description": "Only 'primary' is admissible for projection. 'secondary' records are retained for provenance but may not be rendered.",
          "enum": ["primary", "secondary"]
        },
        "supportingSources": {
          "description": "Additional primary sources used when one URL cannot support both product behaviour and a registry-derived version or metric.",
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/supportingSource" }
        },
        "observedAt": { "$ref": "#/$defs/date" },
        "retrievedAt": { "$ref": "#/$defs/date" },
        "expiresAt": { "$ref": "#/$defs/date" },
        "historical": {
          "description": "True when the observation describes a release line that is no longer current. A historical record may never support a current headline, ratio, or superiority claim (REQ-006).",
          "type": "boolean",
          "default": false
        },
        "historicalReason": { "type": "string", "minLength": 1 },
        "supersededBy": {
          "description": "Record id carrying the current release line, when this record is historical.",
          "type": "string"
        },
        "methodology": { "type": "string", "minLength": 1 },
        "metrics": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/metric" }
        },
        "comparability": {
          "description": "What this record may and may not be compared against, in plain language.",
          "type": "string",
          "minLength": 1
        },
        "operations": {
          "description": "Operation-level behaviour of this product's first-party surface, as read from the record's source at the record's version. The comparison page projects these cells directly, so a behaviour claim is bound to a version, a primary source, and an expiry instead of living as an inline constant.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "discovery": { "$ref": "#/$defs/operationCell" },
            "paging": { "$ref": "#/$defs/operationCell" },
            "edits": { "$ref": "#/$defs/operationCell" },
            "crs": { "$ref": "#/$defs/operationCell" },
            "capabilityNegotiation": { "$ref": "#/$defs/operationCell" },
            "planning": { "$ref": "#/$defs/operationCell" },
            "rendering": { "$ref": "#/$defs/operationCell" },
            "lifecycle": { "$ref": "#/$defs/operationCell" }
          }
        }
      },
      "allOf": [
        {
          "if": { "properties": { "historical": { "const": true } }, "required": ["historical"] },
          "then": { "required": ["historicalReason"] }
        }
      ]
    }
  }
}
