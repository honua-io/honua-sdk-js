# Honua Cloud Spatial Analytics Workbench

Fixture-backed sample for issue #58. It exercises AOI selection, process metadata discovery, async job state, materialized outputs, linked map/table/chart/filter state, workspace export, cache policy, and the explicit #66 indexed analytics gap.

Run locally:

```sh
npm run demo:spatial-analytics
```
