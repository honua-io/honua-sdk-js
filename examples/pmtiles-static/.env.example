# The PMTiles Static Quickstart needs no configuration — the archive is a
# static asset served next to the app (public/basemap.pmtiles). Point a real
# deployment at any PMTiles URL on object storage by editing src/main.ts.
