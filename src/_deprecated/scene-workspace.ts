/**
 * @deprecated Moved to `@honua/app-platform/scene-workspace`, which is now a
 *   `beta` surface. This re-export is available through 0.1.x and will be
 *   removed in 0.2.0 — the forwarder's deprecation window is unchanged by the
 *   promotion. Update imports to `@honua/app-platform/scene-workspace`.
 */
export * from "../scene-workspace/index.js";
